<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.viewpager.widget.ViewPager
        android:id="@+id/viewPager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:overScrollMode="@integer/overScrollMode">

    </androidx.viewpager.widget.ViewPager>

    <!-- Gradient overlay for visualizer (top: transparent, bottom: semi-transparent black) -->
    <View
        android:id="@+id/visualizerGradientOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/gradient_visualizer_overlay"
        android:visibility="gone"
        tools:visibility="visible" />

    <!-- Audio Visualizer Overlay -->
    <code.name.monkey.retromusic.views.AudioVisualizerView
        android:id="@+id/audioVisualizer"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="32dp"
        android:alpha="0.8"
        android:visibility="gone"
        tools:visibility="visible" />

    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/cover_lyrics"
        android:name="code.name.monkey.retromusic.fragments.player.CoverLyricsFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center_vertical" />

    <com.bosphere.fadingedgelayout.FadingEdgeLayout
        android:id="@+id/fading_edge_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:fel_edge="top|bottom"
        app:fel_size_bottom="100dp"
        app:fel_size_top="100dp">

        <code.name.monkey.retromusic.lyrics.CoverLrcView
            android:id="@+id/lyricsView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_margin="16dp"
            android:visibility="gone"
            app:layout_behavior="com.google.android.material.appbar.AppBarLayout$ScrollingViewBehavior"
            app:lrcLabel="@string/no_lyrics_found"
            app:lrcNormalTextSize="28sp"
            app:lrcPadding="24dp"
            app:lrcTextGravity="center"
            app:lrcTextSize="32sp"
            app:lrcTimelineColor="@color/transparent"
            tools:visibility="visible" />
    </com.bosphere.fadingedgelayout.FadingEdgeLayout>

    <!-- Visualizer toggle button -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/visualizerToggle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|end"
        android:layout_margin="16dp"
        android:contentDescription="@string/toggle_visualizer"
        app:fabSize="mini"
        app:srcCompat="@drawable/ic_equalizer" />
</FrameLayout>